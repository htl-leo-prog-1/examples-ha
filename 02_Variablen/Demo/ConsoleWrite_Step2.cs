/*--------------------------------------------------------------
 *				HTBLA-Leonding / Class: xABIF
 *--------------------------------------------------------------
 *              Herbert Aitenbichler 
 *--------------------------------------------------------------
 * Description: Demo - Turmrechnen
 * Ausgabe einer Zeile mit der Platzhaltersyntax
 *--------------------------------------------------------------
*/

using System;

int wert=47;

Console.WriteLine($"{ wert } * 2 = { wert * 2}");
wert = wert * 2;

Console.WriteLine($"{wert} * 3 = { wert * 3}");
wert = wert * 3;

Console.WriteLine($"{wert} * 4 = { wert * 4}");
wert = wert * 4;

Console.WriteLine($"{wert} / 2 = { wert / 2}");
wert = wert / 2;

Console.WriteLine($"{wert} / 3 = { wert / 3}");
wert = wert / 3;

Console.WriteLine($"{wert} / 4 = { wert / 4}");
wert = wert / 4;
